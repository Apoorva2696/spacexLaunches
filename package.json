{
  "name": "my-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cd client && npm install && npm run build && cd ../server && npm install && npm run start"
  },
  "author": "",
  "license": "ISC"
}
